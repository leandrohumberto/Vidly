@model Vidly.Models.Customer
@{
    ViewBag.Title = @Model.Name;
}

<h2>@Model.Name</h2>

<ul>
    <li>@Model.MembershipType.Name</li>
    @if (Model.Birthdate.HasValue)
    {
        <li>@Model.Birthdate.GetValueOrDefault().ToString("d")</li>
    }
</ul>

